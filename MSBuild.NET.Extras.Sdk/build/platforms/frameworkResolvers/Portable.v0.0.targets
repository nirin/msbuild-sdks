<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

	<ImportGroup>
		<Import Project="$(MSBuildThisFileDirectory)Portable.v4.0.targets" />
		<Import Project="$(MSBuildThisFileDirectory)Portable.v4.5.targets" />
		<Import Project="$(MSBuildThisFileDirectory)Portable.v4.6.targets" />
	</ImportGroup>

</Project>