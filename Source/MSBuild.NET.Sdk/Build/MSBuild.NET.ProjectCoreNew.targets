<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<ImportLanguageTargets Condition="'$(ImportLanguageTargets)' == ''">true</ImportLanguageTargets>
		<ImportPlatformTargets Condition="'$(ImportPlatformTargets)' == ''">true</ImportPlatformTargets>
		<ImportFrameworkTargets Condition="'$(ImportFrameworkTargets)' == ''">true</ImportFrameworkTargets>
	</PropertyGroup>

	<ImportGroup Condition="'$(ImportLanguageTargets)' == 'true'">
		<!-- We need to init SourceLanguageName, etc. properties. -->
		<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.LanguageInference.props"/>
		<Import Project="$(MSBuildThisFileDirectory)..\Core\Languages\MSBuild.$(SourceLanguageName).props"/>
		<Import Project="$(MSBuildThisFileDirectory)..\Core\Languages\MSBuild.$(SourceLanguageName).targets"/>
	</ImportGroup>

	<ImportGroup Condition="'$(ImportFrameworkTargets)' == 'true' AND '$(TargetFramework)' != ''">
		<!-- We need to init _SdkFrameworkName, etc. properties. -->
		<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.FrameworkInference.props"/>
		<Import Project="$(MSBuildThisFileDirectory)..\Core\Frameworks\MSBuild.$(_SdkFrameworkName).props"/>
		<Import Project="$(MSBuildThisFileDirectory)..\Core\Frameworks\MSBuild.$(_SdkFrameworkName).targets"/>
	</ImportGroup>

	<ImportGroup Condition="'$(ImportPlatformTargets)' == 'true' AND '$(TargetPlatform)' != ''">
		<!-- We need to init _SdkPlatformName, etc. properties. -->
		<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.PlatformInference.props"/>
		<Import Project="$(MSBuildThisFileDirectory)..\Core\Platforms\MSBuild.$(_SdkPlatformName).props"/>
		<Import Project="$(MSBuildThisFileDirectory)..\Core\Platforms\MSBuild.$(_SdkPlatformName).targets"/>
	</ImportGroup>

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

</Project>