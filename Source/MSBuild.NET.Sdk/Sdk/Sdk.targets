<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<CustomProjectCoreTargets>$(MSBuildThisFileDirectory)..\Build\MSBuild.NET.Sdk.targets</CustomProjectCoreTargets>
		<CustomProjectExtensionTargets>$(MSBuildThisFileDirectory)Sdk.Extensions.targets</CustomProjectExtensionTargets>
	</PropertyGroup>

	<Import Project="Sdk.targets" Sdk="MSBuild.Core.Sdk"/>

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

</Project>