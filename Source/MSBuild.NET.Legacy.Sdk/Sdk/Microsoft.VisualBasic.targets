<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<Import Project="Microsoft.Managed.props"/>

	<!-- When we are doing a multi-targeting build, this import is redirected to $(MSBuildThisFileName).MultiTargeting.targets. -->
	<Choose>
		<When Condition="'$(IsOuterBuild)' == 'true'">
			<PropertyGroup>
				<MicrosoftVisualBasicTargets>$(MSBuildThisFileName).MultiTargeting.targets</MicrosoftVisualBasicTargets>
			</PropertyGroup>
		</When>
		<Otherwise>
			<PropertyGroup>
				<MicrosoftVisualBasicTargets>$(MSBuildThisFileName).CurrentVersion.targets</MicrosoftVisualBasicTargets>
			</PropertyGroup>
		</Otherwise>
	</Choose>

	<Import Project="$(MicrosoftVisualBasicTargets)"/>

	<Import Project="Microsoft.Managed.targets"/>

</Project>