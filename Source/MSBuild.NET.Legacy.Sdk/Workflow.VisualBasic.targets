<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<Choose>
		<When Condition="'$(MSBuildAssemblyVersion)' == ''">
			<PropertyGroup>
				<WorkflowVisualBasicTargetsPath>$(MSBuildLegacySdkPath)Inbox\$(MSBuildThisFile)</WorkflowVisualBasicTargetsPath>
			</PropertyGroup>
		</When>
		<Otherwise>
			<PropertyGroup>
				<WorkflowVisualBasicTargetsPath>$(MSBuildLegacySdkPath)Current\$(MSBuildThisFile)</WorkflowVisualBasicTargetsPath>
			</PropertyGroup>
		</Otherwise>
	</Choose>

	<Import Project="$(MSBuildLegacySdkPath)Extensions\$(MSBuildThisFile)" Condition="'$(ImportLegacyExtensionTargets)' == 'true'"/>
	<Import Project="$(WorkflowVisualBasicTargetsPath)" Condition="'$(ImportLegacyExtensionTargets)' != 'true'"/>

</Project>