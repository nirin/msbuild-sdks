<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<Choose>
		<When Condition="'$(MSBuildAssemblyVersion)' == ''">
			<PropertyGroup>
				<WorkflowTargetsPath>$(MSBuildLegacySdkPath)Inbox\$(MSBuildThisFile)</WorkflowTargetsPath>
			</PropertyGroup>
		</When>
		<Otherwise>
			<PropertyGroup>
				<WorkflowTargetsPath>$(MSBuildLegacySdkPath)Current\$(MSBuildThisFile)</WorkflowTargetsPath>
			</PropertyGroup>
		</Otherwise>
	</Choose>

	<Import Project="$(MSBuildLegacySdkPath)Extensions\$(MSBuildThisFile)" Condition="'$(ImportLegacyExtensionTargets)' == 'true'"/>
	<Import Project="$(WorkflowTargetsPath)" Condition="'$(ImportLegacyExtensionTargets)' != 'true'"/>

</Project>